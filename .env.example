# PTX Chassis Connection Settings
# Copy to .env and fill in your values. Do not commit .env.
PTX_HOST=<your-ptx-hostname-or-ip>
PTX_USER=<your-username>
PTX_PASSWORD=<your-password>
PTX_PORT=22

# Optional: Use SSH key authentication instead of password
# PTX_SSH_KEY=/path/to/your/ssh/private/key

# Optional: Override path to tools config (default: config/tools.yml)
# PTX_TOOLS_CONFIG=/path/to/tools.yml

# Optional: CLI invocation. Default: "cli <command>" (e.g. cli show system software list).
# PTX_CLI_INVOKE=cli-quoted  → "cli 'command'" (for pipes/special chars only)
